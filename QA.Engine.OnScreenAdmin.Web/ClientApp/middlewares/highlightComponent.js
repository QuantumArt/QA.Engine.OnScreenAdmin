// import * as actionTypes from '../actions/actionTypes';

// const highlighter = store => next => action => {
//   if(action.type !== actionTypes.SELECT_COMPONENT)
//     return next(action);
//   let onscreenId = action.id;
//   const element = document.querySelector(`[data-qa-component-on-screen-id="${onscreenId}"]`);
//   if(element) {

//   }
// }